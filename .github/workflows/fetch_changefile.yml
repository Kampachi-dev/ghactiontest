name: fetch changefile
on:
  push:
    paths:
      - '**.md'
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v3
        with:
          fetch-depth: 0
        run: git diff --name-only ${{ github.event.before }} ${{ github.sha }} | xargs